# Copy this file to .env for local development:
#   cp .env.example .env
#
# In production, do NOT use a .env file — set these in your hosting
# provider's "Environment Variables" panel instead (Vercel / Render /
# Railway / Fly.io / etc.).

# ---- Required ----
GEMINI_API_KEY=
GROQ_API_KEY=

# ---- Search engines ----
# A SearXNG instance with JSON output enabled. Many public instances
# disable format=json to deter scraping — host your own, or pick one
# from https://searx.space that allows it. Leave blank to search with
# DuckDuckGo only.
SEARXNG_URL=
DUCKDUCKGO_URL=https://html.duckduckgo.com/html/

# ---- Server ----
PORT=3000
# Set to your frontend's exact origin in production, e.g. https://app.example.com
ALLOWED_ORIGIN=*

# ---- Models ----
# Verify these are still current before deploying — check each
# provider's docs, since model names change over time.
# https://ai.google.dev/gemini-api/docs/models
# https://console.groq.com/docs/models
GEMINI_MODEL=gemini-2.0-flash
GROQ_MODEL=llama-3.3-70b-versatile

# ---- Tuning ----
MAX_SOURCES=5
FETCH_TIMEOUT_MS=8000
CONTENT_CHAR_LIMIT=2500
CACHE_TTL_MINUTES=10
